use std::sync::{Arc};

use components::{Renderable};

pub struct RendererColor;

impl RendererColor {
    pub fn new() -> RendererColor {
        RendererColor
    }

    pub fn render(&mut self, renderable: Arc<Renderable>) {

    }

    pub fn init_vertex() {

    }
}

use std::sync::{Arc};

use components::{Renderable};

pub struct RendererTex2;

impl RendererTex2 {
    pub fn new() -> RendererTex2 {
        RendererTex2
    }

    pub fn render(&mut self, renderable: Arc<Renderable>) {

    }

    pub fn init_vertex() {

    }
}
